apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: contour # This exact string must match the spec.gatewayClassName inside your Gateway
spec:
  controllerName: projectcontour.io/gateway-controller